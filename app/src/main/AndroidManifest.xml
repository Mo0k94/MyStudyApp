<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mystudyapp">

    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="q">
        <activity android:name=".AndExAlertActivity"></activity>
        <activity android:name=".IonAlertActivity" />
        <activity
            android:name=".ImageSlideActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".GligerActivity" />
        <activity android:name=".TedBottomPicker" />
        <activity android:name=".MatisseActivity" />
        <activity android:name=".ImagePickerLibraryActivity" />
        <activity android:name=".ClearEditTextActivity" />
        <activity android:name=".SimpleDialogActivity" />
        <activity android:name=".FullScreenImageActivity" />
        <activity android:name=".textGradientActivity" />
        <activity android:name=".textWriterActivity" />
        <activity android:name=".kotlin.KotlinActivity" />
        <activity android:name=".StepperActivity" />
        <activity android:name=".GetVersionActivity" />
        <activity android:name=".HorizontalChartActivity" />
        <activity android:name=".ChartActivity" />
        <activity android:name=".activities.TedPermissionActivity" />
        <activity android:name=".secretAcitivyty" />
        <activity android:name=".GwangjuUniv.GJBoardActivity" />
        <activity android:name=".GwangjuUniv.GjMainActivity" />
        <activity android:name=".GwangjuUniv.RegisterActivity" />
        <activity android:name=".GwangjuUniv.LoginUiActivity" />
        <activity android:name=".activities.BoardInsertActivity" />
        <activity android:name=".activities.ImageListViewActivity" />
        <activity android:name=".activities.getServerImageActivity" />
        <activity android:name=".activities.Selector_ImageActivity" />
        <activity android:name=".RoomTestActivitiy" />
        <activity android:name=".CheckListViewActivity" />
        <activity android:name=".activities.HTMLParserActivity" />
        <activity android:name=".activities.Retrofit2Activity" />
        <activity
            android:name=".activities.CardViewActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.addViewActivity" />
        <activity android:name=".activities.RecyclerViewActivity" />
        <activity android:name=".activities.AsyncTaskActivity" />
        <activity android:name=".activities.ThreadActivity" />
        <activity android:name=".activities.GalleryActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".activities.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".activities.GeoIpActivity" />
        <activity android:name=".activities.WeatherApiActivity" />
        <activity android:name=".activities.ViewPagerExamActivity" />
        <activity android:name=".activities.ViewPagerActivity" />
        <activity android:name=".activities.ImageFragmentActivity" />
        <activity android:name=".activities.FragmentExamActivity" />
        <activity android:name=".activities.ListViewExamActivity" />
        <activity android:name=".activities.ColorFragmentActivity" />
        <activity android:name=".BasketBallActivity" />
        <activity android:name=".activities.LifeCycleActivity" />
        <activity android:name=".activities.Memo2Activity" />
        <activity
            android:name=".activities.MemoActivity"
            android:label="@string/title_activity_memo"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.WeatherActivity" />
        <activity android:name=".activities.WebBrowserActivity" />
        <activity
            android:name=".ListViewActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>

</manifest>